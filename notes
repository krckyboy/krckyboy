Commands used:

To use sass in the project:
npm install node-sass --save-dev 

Live server to install on the computer:
npm install live-server -g


For example, jQuery:
npm install jquery --save

------------------------------------------

Responsive trick;
 -Tweak font-size (initially 62.5%) for the global change in responsiveness - padding, margins, everything. 
 

------------------------------------------
 Todo

 -- 1. Dodati mozda jos jedan container za slideshow kako bi mogao da tu rasiris sliku, a pritom i da to bude position relative za arrows
 -- 2. Proveriti components za slideshow